# 健康巡检 · 2026-09-06（全量扫描 · Full Sweep）

> **触发**：用户 `/flow-health 全量扫描`
> **模式**：完整审计（报告模式，只产报告+登记，不改代码）
> **上次**：97/100（2026-09-01 FULL-SWEEP）· 本周期间变更：TD-023 清零提交（09-02）+ user-guide-sync / dsh-flow-kit-sync（09-03）+ l3-prompt-loop-fix 归档（09-05）
> **工具**：jscpd 5.1.2（npx）· bats-core 1.13.0（npx）· shellcheck（error 门禁）· node v22.23.2 · python3 3.12

---

## 综合分

- **当前：94/100** 🟢（与 09-01 同口径计分惯例：🔴 -10 / 🟡 -3 / 🟢 -1）
- 上次：97/100（2026-09-01）
- 趋势：**↓ 3**（无 🔴、无代码级回归；扣分 = 新增 1 项 🟡 flaky 测试 −3 + 新增 🟢 SC2034×2 −1 − M10 销账回补 +1）

### 评分构成

| 项 | 扣分 | 说明 |
|---|---|---|
| 🟢 jscpd bash 重复（延续项） | -1 | 同口径复测持平：09-01 基线 9 clones / 0.544% → 今日 9 clones / 0.536%（组清单逐对一致，无新增组） |
| 🟢 归档残留目录 ×2（同类复发） | -1 | `.specs/l3-prompt-loop-fix/PROGRESS.md` + `.specs/correction-hygiene-state-guard/PROGRESS.md`（归档后遗留，git 已忽略） |
| 🟢 l3-prompt.sh:94 未用局部变量 sev/summ（新） | -1 | SC2034×2，l3-prompt-loop-fix（T02 提取器）引入的死局部量 |
| 🟡 **F-1 测试偶发失败（新 · 唯一实质新项）** | -3 | test_l3_pipeline_fix.bats:440-441 `printf | grep -qF` SIGPIPE 141：首轮全量 1 not-ok，复跑全量 803/803 绿、单文件 41/41 绿 → 满负荷偶发 flaky |
| ~~M10（29 号 M0 块 ~90 行）~~ | ~~-1~~ | **销账**：活跃记忆（CONTEXT/LESSONS/CHANGELOG）已无登记，不再计分 |

## 语法门禁 · 步骤 2.6

```
244 个脚本 · 0 错误 ✅（全仓 find .sh，排除 .git/node_modules/第三方 brooks-lint plugin/.omo；09-01 为 242 个 · 0 错误）
```

## 测试状态（比 09-01 的 764 基线 +39 → 803 总用例）

- **首轮全量**：801 pass + 1 skip（既有 validate AC-4 注明 skip）+ **1 not ok**（test_l3_pipeline_fix.bats:440 · T04 AC-1 · status 141 = SIGPIPE）→ exit 1
- **全量复跑**：803 ok（含 1 skip）/ 0 not ok ✅
- **单文件重跑**：test/test_l3_pipeline_fix.bats 41/41 · rc=0 ✅
- **判定**：F-1 为满负荷下偶发 flake（机制：`printf` 管道 + `grep -qF` 命中即退 → writer SIGPIPE 141，pipefail 下管道返回 141；单跑时调度时序不触发）
- 双源测试镜像 `test/ ↔ flow-kit-bundle/test/`：diff -rq 一致 ✅（make check-test-sync 绿）
- l3-prompt 四副本 md5 硬断言（T06 AC-6，含 ~/.claude 副本）：pass ✅
- dsh-flow-kit JS 单测：4 文件 **20/20 pass**（flow-state 8 / hook-bridge 6 / l2-review 4 / skill-loader 2）
- Python（deck 生成器 + pptx 工具）：py_compile 全过 ✅

## 6 维生产代码风险（热点：l3-prompt.sh×6 commits / flow-state.js×5 / common.sh×4 / l2-detect×3 / 33-flow-active×3 —— 均经本周期 L2×6+L3×7 盲审归档）

| 维度 | 🔴 | 🟡 | 🟢 | 备注 |
|---|---|---|---|---|
| R1 认知过载 | 0 | 0 | 1 | l3-prompt.sh 369 行拆 6 主函数（_l3_build_prompt ~117 / _l3_extract_prior_findings ~87 / _l3_inject_context ~74，均带用法注释单职责）；全仓无 >550 行文件、无 >200 行单函数；M10 结转销账 |
| R2 变更传播 | 0 | 0 | 0 | HOOK_MODULE_NAMES/PRESET_MAP 单一源；l3-prompt 四副本 md5 断言（T06 AC-6）+ 双源测试 diff 门禁 |
| R3 知识重复 | 0 | 0 | 1 | jscpd 9 组与 09-01 基线逐对相同（无新增组）；md 内嵌 bash 示例 ×4 + guard idiom ×5，均 ≤21 行成对（TD-004/019 已文档化类） |
| R4 偶然复杂 | 0 | 0 | 0 | _l3_build_prompt 用 jq --arg 构造 JSON 契约；JIT source common.sh（fail-open）有注释理由 |
| R5 依赖混乱 | 0 | 0 | 0 | hooks→stop/lib 严格单向；l3-review 薄入口 source 4 叶子；新 l3-prompt 为纯 leaf，无环 |
| R6 领域扭曲 | 0 | 0 | 0 | 命名前缀守约（_l3_/_fk_/check_…）；新函数延续 _l3_ 前缀 |

## 6 维测试代码风险（抽样：test_l3_pipeline_fix（41 tests 新增主体）/ test_correction_hygiene / test_flow_active_integrity / install 系列 + node .test.mjs）

| 维度 | 🔴 | 🟡 | 🟢 | 备注 |
|---|---|---|---|---|
| T1 测试晦涩 | 0 | 0 | 0 | AC-N / T0N 场景命名 + fixture 驱动 |
| T2 测试脆弱 | 0 | **1** | 0 | **F-1**：`printf | grep -qF` SIGPIPE 141 偶发（440-441）；断言真实（AC-1 字节界+顺序），失败模式为管道实现脆弱 |
| T3 测试重复 | 0 | 0 | 1 | setup 样板沿用 bats 惯例（BATS_ROOT 已统一） |
| T4 Mock 滥用 | 0 | 0 | 0 | 0 mock，真子进程 + fixture 驱动 |
| T5 覆盖率幻觉 | 0 | 0 | 0 | T06 md5 四副本等硬断言；L-087 教训（sed 截断锚）已吸收改行为断言 |
| T6 架构错配 | 0 | 0 | 0 | unit（lib 函数）与 integration（e2e/AC）分层合理 |

## 架构图

```mermaid
graph TD
    SS[stop-hook 接线] --> RUN[00-gate.sh runner]
    RUN --> MODS[stop 主模块 ×15]
    MODS --> LIB[stop/lib/*：common 单 hub + correction-file / l2-detect / l3-review 族 / weak-model / flow-kit-artifacts / banner …]
    LR[l3-review.sh 薄入口] --> L3P[l3-prompt.sh 369L 纯 leaf] & L3A[l3-api.sh] & L3T[l3-truncate.sh] & L3D[l3-done.sh]
    RES[session-start/flow-kit-resume.sh] --> LIB2[resume/banner lib]
    PRE[pre-tool-use ×5] -->|只读| LIB
    INS[install_hooks.sh + lib/*] --> PATHS[paths.sh 平台路径]
    PKG[package-flow-kit.sh] --> FB[flow-kit-bundle/]
    DSH[dsh-flow-kit/lib/index.js] --> FS[flow-state.js] & L2R[l2-review.js] & HB[hook-bridge.js → shell hook 套件] & SL[skill-loader.js]
```

**判定**：全 🟢——与 09-01 拓扑一致：单一 hub（stop/lib）+ 严格单向无环；本周期新增（l3-prompt.sh 叶子、JS 插件四模块）均落在叶子层。

## 冗余巡检 · 步骤 2.5

**工具**：jscpd 5.1.2（make-dup 口径 ignore）+ shellcheck + AI grep 函数引用图 + node/ESM 人工核对（JS 仅 4 lib + 4 test 文件，规模小未跑 knip，已标 ⚠️）

| 维度 | 🔴 | 🟡 | 🟢 | 元 |
|---|---|---|---|---|
| 字面重复块 | 0 | 0 | 9 | bash 9 clones / 78 dup 行 / 14565 行 = **0.536%**（同口径 09-01 复测 0.544% → 持平微降）；md 结构性 26.6%（同口径旧 25.7%，TD-004 已文档化） |
| 未用导出 | 0 | 0 | 0 | bash：11 个候选全为 def+调用同文件/跨文件 source 假阳性（同 09-01 结论）；JS：index.js 消费全部 4 模块导出，20 单测绿 |
| 未用依赖 | — | — | — | N/A（纯 bash + jq/shellcheck 外部工具；dsh-flow-kit 仅 peer @deepseek-ai/cordis） |
| 死代码·孤立文件 | 0 | 0 | 2 | **SC2034×2**：l3-prompt.sh:94 未用局部量 sev/summ（新）；归档残留 PROGRESS.md ×2（.specs/l3-prompt-loop-fix/ + .specs/correction-hygiene-state-guard/） |

> 09-01 报告内「5 clones / 0.35%（35/9935 行）」与本日同命令复测（9 clones / 0.544%）不一致——口径/工具版本漂移所致；本报告趋势一律用**同命令复测**（09-01 基线同为 9 clones），结论不受影响。
> ⚠️ JS 侧未跑 knip（纯 bash 仓 + 8 文件插件），按规模人工核对。

**发现清单（4 组延续、无新增组，全 🟢）**：
- README.md:bash:26-46 ↔ package-flow-kit.sh:200-220 · 21L 用法示例（文档↔代码，09-01 已有）
- prompts/4-dev ↔ reference/commit-protocol · 12L（抽取引用模式，TD-004）
- 5-test/6-review/7-integration · 7L ×2（入场 jq 段，结构性已文档化）
- pre-tool-use guard idiom ×3 + 27↔28 配对 9L + common↔install_hooks 6L + l2-detect↔l3-review 11L（guard/惯用段，TD-019 类）

## shellcheck（门禁 error 级）

- **error 级：0**（make lint 门禁绿 ✅）
- **warning 级：9 处**（同批文件 vs 5cc4502 基线 19 处 → 净减 10，TD-023 清账未复活）：
  - 8 处 pre-existing 已知可接受池：SC1010×2（`then` 作变量名保留字）、SC1083×1（字符类内 `{` 字面量）、SC2010×1（ls|grep 启发式）、SC2053×1（`[[ == ]]` RHS 未引号——**有意 glob**，建议补注释）、SC2088×2（单引号 `~/.claude/` 字面量模式）
  - **1 处新增**：SC2034×2 l3-prompt.sh:94 sev/summ（与冗余巡检同源，建议删）
- note 级（informational）~80 条：SC2015×55 / SC2016×12 / SC2012×5 / SC2086×4 等，多为惯用 `&& ||` 防御与文档示例——维持 09-01 判定（非阻塞）

## 技术债状态

- **TD 表**：TD-001…TD-023 全 ✅ 无复活；本次**新增登记 TD-024**（🟡 Scheduled · F-1 flaky 测试，见 CONTEXT.md）
- **LESSONS**：本报告追加 2 条 M-health 观察（flake 管道模式 / 归档残留模式）

## 行动建议（按优先级）

- 🔴 Critical · **无**（不开 health-fix CHANGE）
- 🟡 Scheduled · 本季度：
  1. **TD-024 / F-1**：test_l3_pipeline_fix.bats:440-441 去 `grep -q` 管道 —— `[[ "$out" == *"=== CHANGELOG.md ==="* ]]` 或 `grep -F … >/dev/null`，一行修，可随任意 health-fix change
- 🟢 Monitored · 仅记录：
  1. l3-prompt.sh:94 删未用 `sev summ`
  2. 删残留目录 `.specs/l3-prompt-loop-fix/`、`.specs/correction-hygiene-state-guard/`（各含 1 个 ignored PROGRESS.md）
  3. 归档流程改进：归档移位后清理源目录残留 PROGRESS.md（本次为第 2 次同类复发）
  4. flow-kit-artifacts.sh:267 SC2053 补「有意 glob」注释；克隆 9 组延续观察；md 结构性 26.6% 趋势观察

## 与上次对比（2026-09-01 · 97/100）

- **修了**：TD-023 全量清账（09-02，复核 9 处旧 warning 消失：package-flow-kit SC2034×2、21-memory、flow-kit-artifacts、weak-model-compliance、00-gate name 等）；l3-prompt.sh SC2155×3 + agent_response SC2034 随 T01-T06 拆分清理；09-01 遗留 2 孤儿目录 + junk 已清
- **退化了**：无代码级退化；**新出现 1 项 🟡**：F-1 测试偶发 SIGPIPE（门禁可复现性隐患，非功能失效）
- **新出现 🟢**：l3-prompt.sh:94 SC2034×2；归档残留 PROGRESS.md ×2（同类复发，源自 09-01/09-05 归档移位）
- 测试基线：764（09-01）→ **803**（+39，l3-prompt-loop-fix 补齐）；dsh 插件 JS 单测 20/20

---

*下次巡检建议：2026-10-01（周期性 health 模式即可）*
