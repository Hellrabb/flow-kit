# 健康巡检 POST-FIX · 2026-09-06

> 承接 `.specs/health/2026-09-06-HEALTH.md`（94/100）行动建议，用户确认「没风险的话都做完」后当场闭环 3 项 🟡/🟢 遗留。

## 1. TD-024 / F-1 · 测试偶发 SIGPIPE（🟡 → ✅）

- **根因**：`printf … | grep -q/-qF/-qE` 读取端命中即退 → 满负荷下管道 writer 收 SIGPIPE，pipefail 下整条管道 rc=141（09-06 首轮全量 1 not-ok）
- **修复**：`test/test_l3_pipeline_fix.bats` 全文件 **40 处** `grep -q*` 统一改为 `grep/-F/-E … >/dev/null`（去掉提前退出；rc 语义不变）；镜像同步 `flow-kit-bundle/test/`（diff 一致）
- **验证**：单文件 41/41 ×2 绿（含 T04 AC-1 原失败用例）→ 修复后全量复跑 **803 ok（含 1 既有 skip）/ 0 not ok** —— 满负载下 F-1 不再复现

## 2. l3-prompt.sh:94 未用局部量（🟢 → ✅）

- `_l3_extract_prior_findings` 中 `local section="" line sev summ file` 的 `sev summ` 全文件零引用（SC2034×2）→ 删除
- 五副本同步：`flow-kit-bundle/hooks/stop/lib/`（规范源）+ `.claude/hooks/stop/lib/` + `dist/dsh-flow-kit/hooks/` + `dist/dsh-flow-kit/vendor/…/hooks/` + `~/.claude/hooks/`（md5 唯一=1，T06 AC-6 断言绿）
- shellcheck：SC2034 归零（该文件仅剩 note 级 SC2015×3/SC2012×1）

## 3. 归档残留目录（🟢 → ✅）

- 删除 `.specs/l3-prompt-loop-fix/`、`.specs/correction-hygiene-state-guard/`（各含 1 个 ignored PROGRESS.md；正式产物均在 `.specs/archive/2026-09-05-l3-prompt-loop-fix/` 与 `.specs/archive/2026-09-01-correction-hygiene-state-guard/`，已确认完整）
- 流程观察仍有效：归档动作应补「源目录清空/删除」步骤（LESSONS 2026-09-06 行）

## 验证汇总

- 全量 bats：803 ok / 0 not ok / 1 skip ✅（修复前首轮为 801 ok + 1 skip + 1 flake）
- test 双源 diff：一致 ✅
- l3-prompt 五副本 md5：唯一 ✅
- 工作区：提交后干净 ✅

*遗留观察：`grep -q` 提前退出惯用法在其他 bats 文件仍存在（输出体量小、未实测触发），可留作后续统一治理候选。*
