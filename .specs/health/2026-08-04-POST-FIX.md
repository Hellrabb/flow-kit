# 健康巡检 · 2026-08-04（health-fix post-fix 验证）

> **触发**: health-fix-2026-08 commit `8e391e9` 后验证 · 用户要求 commit 后重扫
> **模式**: 快速体检（仅验证 Critical 修复 + 基线恢复 · 不重跑全量 6R+6T 内置评分）
> **上次**: 89/100（2026-08-03 · `2026-08-03-FULL-SWEP.md`）

---

## 综合分

- **当前：99/100** 🟢
- 上次：89/100（2026-08-03）
- 基线：98/100（2026-07-25）
- **趋势：↑ 10**（从 89 恢复并超越 baseline 98）

### 评分构成

| 项 | 分值 | 说明 |
|---|---|---|
| 🔴 install_hooks.sh:97 PROJECT_DIR_NAME 回归 | **0**（已修复） | commit `8e391e9` · Fix A paths.sh 自加载守卫 |
| 🟢 jscpd 0.47%（6 clones · 1 新增 · 防御性 guard idiom） | **-1** | common.sh:270 ↔ install_hooks.sh:81 · 6 行 |
| 基线 🟢 l3-review 875 行 watch | **0**（已解决） | final-debt-cleanup 拆分后已消解 |
| **综合** | **99/100** | — |

---

## 语法门禁 · 步骤 2.6

```
74 脚本 · 0 错误 ✅
```

排除: node_modules / .git / brooks-lint/plugin / brooks-tools / .claude/plugins / .omo / .specs/health-fix-2026-08

---

## 冗余巡检 · 步骤 2.5

**工具**: jscpd（已跑 · bash + json）

| 格式 | 文件 | 总行 | Clones | 重复行 | 重复行% | 重复 token% |
|---|---|---|---|---|---|---|
| bash | 64 | 9453 | 6 | 44 | 0.47% | 0.91% |
| json | — | 261 | 2 | 38 | — | — |

### Bash 6 clones（对比上次 5 clones · +1 新增）

| # | 文件 A | 文件 B | 行数 | 状态 |
|---|---|---|---|---|
| 1 | `auto-checkpoint.sh:63` | `independent-review-gate.sh:105` | 6 | 既有 · PreToolUse 模板 |
| 2 | `gate-helpers.sh:69` | `gate-helpers.sh:85` | 7 | 既有 · 内部 |
| 3 | `27-interactive-ui-check.sh:26` | `28-weak-model-compliance.sh:22` | 9 | 既有 · Stop hook 模板 |
| 4 | **`common.sh:270`** | **`install_hooks.sh:81`** | **6** | **🆕 health-fix 引入** · 防御性 guard idiom |
| 5 | `l2-detect.sh:111` | `l3-review.sh:234` | 11 | 既有 · L2/L3 shared mode |
| 6 | `forged-done/check.sh:6` | `tampered-done/check.sh:6` | 6 | 既有 · regression fixture |

**新增 #4 分析**: common.sh:270 的 `init_paths()` 守卫模式（`[ -z "${VAR:-}" ]`）与 install_hooks.sh:81 的新 paths.sh 自加载守卫在结构上相似（同 bash idiom · 不同语义）。**判定 🟢 Minor** — 6 行 boilerplate，不构成知识重复（R3 概念级），不需抽公共函数。

---

## 质量门禁

| 检查 | 结果 |
|---|---|
| `make test` | ✅ **692 ok / 0 fail** / 0 skip（+1 vs baseline 691 · AC-C1 防回归 case）|
| `make lint` | ✅ 0 errors（shellcheck error level） |
| `bash -n`（74 scripts） | ✅ 0 errors |
| 双源 test diff | ✅ identical |

---

## 与上次对比（2026-08-03 → 2026-08-04）

### 修复项

| 项 | 上次 | 本次 |
|---|---|---|
| install_hooks.sh:97 user-scope PROJECT_DIR_NAME 回归 | 🔴 Critical (-10) | ✅ **已修复**（commit 8e391e9 · Fix A paths.sh 自加载守卫）|
| 4 bats test fails（install_coverage + install_dry_run） | 🔴 | ✅ **全绿**（692/0）|
| runtime-edit-guard.sh 安装阻断 | 🔴（副作用） | ✅ **解锁**（L177-183 恢复执行） |
| 主路径 install.sh smoke | — | ✅ exit 0 |

### 持续项

| 项 | 状态 |
|---|---|
| jscpd 🟢 Minor | 持续（0.37% → 0.47% · +1 clone 来自 Fix A guard idiom · 可接受） |
| l3-review.sh 875 行 watch | 已解决（final-debt-cleanup 拆分 · 本次确认未复发） |

### 新出现项

| 项 | 严重度 | 建议 |
|---|---|---|
| common.sh:270 ↔ install_hooks.sh:81 guard idiom 重复（6 行） | 🟢 Minor | 仅记录 · 不需 action（bash idiom · 非业务逻辑） |
| lib/paths.sh 未登记到 CONTEXT.md「既有抽象索引」| 🟢 Minor | 留下次 A-evolve 同步 |

---

## 行动建议

### 🟢 Monitored（仅记录）

- jscpd 总重复率 0.47% — 项目长期在 0.3~0.5% 区间稳定，无需 action
- lib/paths.sh CONTEXT.md 登记缺失 — 下次 A-evolve 同步时补登
- gate_config=all pipeline 中 phase 3/5/7 走 self-certify 而非 oracle L2 — 流程债务（用户已知悉）

### 下次巡检建议

- **日期**: 2026-09-04（1 个月后）或下一个里程碑前
- **重点**: 跟踪 jscpd 趋势 · 确认 paths.sh 已登记 · 检查 gate_config self-certify 是否需补 oracle

---

## 综合结论

**Critical 已闭合。评分 89 → 99（+10 · 超越 baseline 98）。** 项目恢复健康状态。pipeline 通过 gate_config=all（含 3 处 self-certify 的 L2 — 流程债务记录在案）。
